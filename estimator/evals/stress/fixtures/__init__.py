"""Generated attachment fixtures for the stress suite.

The PDFs themselves are NOT committed — they are deterministic and regenerated
on demand by ``build_pdfs`` (``ensure_fixtures``). Only the generator is
versioned.
"""
